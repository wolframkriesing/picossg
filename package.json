{
  "name": "picossg",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node --watch-path src --watch-path build.js build.js",
    "serve": "npx http-server dist -p 8899",
    "test": "npx nodemon --legacy-watch --watch dist --watch expected-dist --watch build.js --ext '*' --exec \"diff -r dist expected-dist\""
  },
  "keywords": [],
  "author": "Wolfram Kriesing",
  "license": "agpl-3.0",
  "description": "",
  "dependencies": {
    "markdown-it": "^14.1.0",
    "nunjucks": "^3.2.4"
  }
}
